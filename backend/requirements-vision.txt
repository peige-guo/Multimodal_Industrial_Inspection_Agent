# Optional Phase-2 vision dependencies.
# Install on top of backend/requirements.txt only when using a real model:
#   pip install -r backend/requirements.txt -r backend/requirements-vision.txt

# Image handling (model input + annotated output).
pillow>=10.3.0

# Local Qwen-VL backend (INSPECTION_DETECTOR=qwen), runs in-process.
# Qwen2.5-VL support requires a recent transformers release.
torch>=2.2.0
torchvision>=0.17.0
transformers>=4.49.0
accelerate>=0.30.0
qwen-vl-utils>=0.0.8

# YOLO detector backend (INSPECTION_DETECTOR=yolo); pulls in torch.
ultralytics>=8.2.0

# Optional: only needed for INSPECTION_DETECTOR=vlm with an OpenAI-compatible
# server (e.g. Ollama or vLLM serving Qwen-VL). Not required for local Qwen.
openai>=1.30.0
